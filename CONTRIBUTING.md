# 贡献指南

感谢你愿意为「安心服药」贡献。本项目的核心原则是：**功能范围受控，以低缺陷率和可测试性优先**。第一版不加入网络、账号、云同步、广告、分析或医疗建议功能。

## 提交要求

- 涉及日程计算、库存预测或提醒编排的改动，**必须**附带对应的自动化测试。
- 改动需通过完整验证：

```bash
.\gradlew.bat clean testDebugUnitTest compileDebugAndroidTestKotlin lintDebug assembleDebug
```

- 每个任务或缺陷修复一个提交，提交信息遵循 `feat:` / `fix:` / `build:` / `docs:` 前缀。

## 禁止事项

- 新增任何网络访问（`INTERNET` 权限、http 客户端等）必须经过新的评审设计，且通过后仍须保持默认离线。
- 不得使用二进制浮点数存储剂量或库存数量。
- 不得把降级的提醒能力伪装成正常状态。

## 代码规范

- 领域规则（日程、库存、状态转换）写成不依赖 Android 的纯 Kotlin，并注入时间来源以便确定性测试。
- 界面使用应用自带的 Compose 主题，不使用动态取色以免降低对比度。
